# 安全策略

巡御运行在服务器安全边界附近，漏洞披露需要比普通应用更谨慎。请不要在公开 Issue、Discussion、PR、日志或截图中提交未修复漏洞、凭据、注册令牌或服务器数据。

## 支持范围

安全修复优先提供给最新稳定版本。项目尚未发布稳定版时，`main` 分支是唯一支持目标。正式发布后，本节会维护具体版本表。

| 版本 | 安全支持 |
|---|---|
| 最新稳定版 | 支持 |
| 更早版本 | 仅在维护公告明确说明时支持 |
| 未修改的 `main` | 发布前阶段尽力支持 |

## 私下报告漏洞

优先使用 GitHub 仓库的 **Security → Report a vulnerability**（Private vulnerability reporting）。如果该入口不可用，请联系仓库所有者并只说明“需要安全报告通道”，不要先发送漏洞细节。

报告最好包含：

- 受影响的版本、提交和安装方式；
- 前置权限与部署拓扑；
- 可重复的最小步骤或测试；
- 可能影响的机密性、完整性和可用性；
- 日志或数据样例（请先去除真实凭据和个人数据）；
- 已知缓解方式以及是否正在被利用。

我们会尽力在 3 个工作日内确认收到，在完成初步复现后同步严重性、修复计划和协同披露时间。时间不是安全保证；复杂问题可能需要更久。

## 特别关注的安全边界

以下问题通常属于高价值报告：

- 未经批准执行修复或绕过自动防御策略；
- 绕过 Helper 的 Unix peer/token 校验或让非 Agent 账号调用特权 Playbook；
- 模型输出、扫描内容或设备消息导致任意命令执行；
- Agent/Controller 身份混淆、跨设备越权或注册令牌重放；
- API Key、主密钥、注册令牌、报告原文或审计数据泄漏；
- 更新、安装包、SBOM 或签名验证绕过；
- 默认部署可从非预期网络访问管理界面；
- 审批记录、审计数据或回滚状态可被未授权篡改。

社工、对不属于报告者的系统进行压力测试、破坏数据、持久化访问以及反向攻击不属于授权研究。

## 处理依赖漏洞

仅报告一个依赖 CVE、但不能说明巡御中可达路径或实际影响的报告，可能被关闭为信息项。请尽量说明受影响组件是否被构建、部署和执行。

## 安全发布

发布流程生成：

- 每个平台的归档文件；
- `SHA256SUMS`；
- SPDX 与 CycloneDX SBOM；
- 由 GitHub Actions OIDC 进行的 Sigstore/Cosign keyless 签名和证明。

安装脚本始终校验 SHA-256，并强制使用 Cosign 验证 Sigstore keyless 签名；任一验证失败都会在安装前终止。旧版 `--require-signature` 参数仅为命令兼容保留，不能关闭或改变这项强制验证。

感谢负责任地帮助我们保护用户。
